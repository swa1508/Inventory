package com.app.test;

import org.springframework.context.ApplicationContext;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;

import com.app.config.AppConfig;
import com.app.model.Student;
import com.app.service.IStudentService;

public class Test1 {
	public static void main(String[] args) {
		
		/*
		 * ApplicationContext context = new
		 * AnnotationConfigApplicationContext(AppConfig.class); Student s =
		 * context.getBean(Student.class); s.setStudentId(1);
		 * s.setStudentName("Swatantra"); s.setStudentAddress("Sagar");
		 * 
		 * IStudentService studentService = context.getBean(IStudentService.class);
		 * studentService.saveStudent(s);
		 */
	}
}
